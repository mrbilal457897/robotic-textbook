/**
 * BackToTopButton Override
 * Hides the default Docusaurus back-to-top button
 * We use our custom BackToTop component instead
 */

import React from 'react';

export default function BackToTopButton(): null {
  // Return null to hide the default Docusaurus back-to-top button
  return null;
}
